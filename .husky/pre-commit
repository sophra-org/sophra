#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run codebase sync
if ! ts-node --project tsconfig.codebase-sync.json codebase-sync.ts sync 'auto-sync'; then
  echo "❌ Codebase sync failed"
  exit 1
fi

exit 0